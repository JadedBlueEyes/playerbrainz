"""
Implement the DateTime<FixedOffset> scalar

The input/output is a string in RFC3339 format.
"""
scalar DateTime

type Query {
  hello: String!
  whoami: User!
}

type User {
  admin: Boolean!
  createdAt: DateTime!
  displayName: String
  id: Int!
  slug: String!
  updatedAt: DateTime!
}